"""Тест эталонного набора.

Проверяет не санитизацию, а пригодность самого эталона: разметка верна,
контрольные суммы ИНН и СНИЛС сходятся, пересечений с демо-данными нет.
Сама логика живёт в sanitizer.control_check — она же используется детектором
как правило сравнения, поэтому модуль остался в пакете, а не переехал сюда.
"""

from sanitizer import control_check, paths


def test_эталон_пригоден():
    код = control_check.main(paths.данные("control_set.json"),
                             paths.данные("demo_dump.sql"))
    assert код == 0, "эталонный набор не прошёл собственную проверку"
